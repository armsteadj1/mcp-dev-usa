# @mcp-dev-usa/shippo

Shippo MCP server for AI tool use.

## Quick Start

```bash
npx @mcp-dev-usa/shippo
```

## Environment Variables

SHIPPO_API_KEY

## Claude Desktop Config

```json
{
  "mcpServers": {
    "shippo": {
      "command": "npx",
      "args": ["@mcp-dev-usa/shippo"]
    }
  }
}
```
