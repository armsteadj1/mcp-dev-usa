# @mcp-dev-usa/stripe

Stripe MCP server for AI tool use.

## Quick Start

```bash
npx @mcp-dev-usa/stripe
```

## Environment Variables

STRIPE_API_KEY

## Claude Desktop Config

```json
{
  "mcpServers": {
    "stripe": {
      "command": "npx",
      "args": ["@mcp-dev-usa/stripe"]
    }
  }
}
```
